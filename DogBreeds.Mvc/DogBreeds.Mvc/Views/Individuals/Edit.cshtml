@model DogBreeds.Mvc.Models.IndividualViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Individual</h4>
<hr />

@Html.Partial("_Messages", Model.Messages)
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Individual.Id" />
            <div class="form-group">
                <label asp-for="Individual.Name" class="control-label"></label>
                <input asp-for="Individual.Name" class="form-control" />
                <span asp-validation-for="Individual.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Individual.BreedId" class="control-label"></label>
                <select asp-for="Individual.BreedId" asp-items="@(new SelectList(Model.Breeds,"Id","FullName"))">
                    <option>Please Select a Breed</option>
                </select>
                <span asp-validation-for="Individual.BreedId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
