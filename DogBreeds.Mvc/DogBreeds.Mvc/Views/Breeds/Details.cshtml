@model DogBreeds.Mvc.Models.BreedViewModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Breed</h4>
    <hr />
    @Html.Partial("_Messages", Model.Messages)
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Breed.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Breed.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Breed.ParentBreed)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Breed.ParentBreed.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Breed.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>

@Html.Partial("_BreedChildren", Model)
